"""
api
"""

# imports

# todo: handle launch either from CLI or from GUI

directory = int
type = str
file_bytes = int

with open('README.md', 'rb') as p_file:
    content = p_file.read()
    print("erngierrgu" + type(content))
    to_bytes
    # while (content):  # if there are still bytes to iterate through
    #   cur_byte = content[0:8]
    #  for i in range(0, 8):
    #     pass
